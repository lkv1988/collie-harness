---
description: "Scan ~/.collie-harness/queue/*.md and schedule pending tasks via CronCreate"
---

# Queue

Scan the `~/.collie-harness/queue/` directory for pending task files and schedule them.

Invoke the `collie-harness:queue` skill:

> Use Skill("collie-harness:queue") to scan the queue directory and schedule any pending tasks.
